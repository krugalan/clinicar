import { themeColors } from "./themeColors";
import { fontsConfigTheme, fontsTheme } from "./themeFonts";

export const customTheme = {
    fontConfig: fontsConfigTheme,
    fonts: fontsTheme,
    colors: themeColors
}